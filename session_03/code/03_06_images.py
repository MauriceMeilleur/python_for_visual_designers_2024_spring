# the filename of our image; we can also drag a file into the code window to get a string with the file path in the code
myImageName = 'black-raspberries.jpg'
# draw the image on the canvas
image(myImageName, (0, 0))